{
  "name": "harrington",
  "version": "1.0.0",
  "private": true,
  "description": "A family learning platform built on Puter.js and the Marble Skill Taxonomy. Static front end (no build step); this manifest exists only for the deploy scripts.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "deploy:site": "node scripts/deploy-puter.mjs",
    "deploy:worker": "node scripts/deploy-worker.mjs"
  },
  "dependencies": {
    "@heyputer/puter.js": "^2.6.0"
  }
}
