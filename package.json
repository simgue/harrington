{
  "name": "homestead",
  "version": "1.0.0",
  "private": true,
  "description": "A mastery-based homeschool platform for ages 5-13, built on Puter.js. Static front end (no build step); this manifest exists only for the deploy scripts.",
  "scripts": {
    "deploy:site": "node scripts/deploy-puter.mjs",
    "deploy:worker": "node scripts/deploy-worker.mjs"
  },
  "dependencies": {
    "@heyputer/puter.js": "^2.6.0"
  }
}
